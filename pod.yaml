apiVersion: v1
kind: Pod
metadata:
  name: flaskpod
spec:
  containers:
  - name: flaskapp
    image: jth7947/flaskapp:latest
    ports:
    - containerPort: 5000
  imagePullSecrets:
    - name: regcred
