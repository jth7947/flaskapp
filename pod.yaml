apiVersion: v1
kind: Pod
metadata:
  name: flask
spec:
  containers:
  - name: flaskapp
    image: jth7947/flaskapp:latest
    ports:
    - containerPort: 5000
  imagePullSecrets:
    - name: docker-pull-secret
